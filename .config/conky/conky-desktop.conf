-- Conky, a system monitor https://github.com/brndnmtthws/conky
--
-- This configuration file is Lua code. You can write code in here, and it will
-- execute when Conky loads. You can use it to generate your own advanced
-- configurations.
--
-- Try this (remove the `--`):
--
--   print("Loading Conky config")
--
-- For more on Lua, see:
-- https://www.lua.org/pil/contents.html

conky.config = {
    alignment = 'top_left',
    background = false,
    border_width = 1,
    cpu_avg_samples = 2,
    default_color = 'white',
    default_outline_color = 'white',
    default_shade_color = 'white',
    double_buffer = true,
    draw_borders = false,
    draw_graph_borders = true,
    draw_outline = false,
    draw_shades = false,
    extra_newline = false,
    font = 'DejaVu Sans Mono:size=12',
    gap_x = 4404,
    gap_y = 1240,
    minimum_height = 5,
    minimum_width = 5,
    net_avg_samples = 2,
    no_buffers = true,
    out_to_console = false,
    out_to_ncurses = false,
    out_to_stderr = false,
    out_to_wayland = false,
    out_to_x = true,
    own_window = true,
    own_window_class = 'Conky',
    own_window_transparent = true,
    own_window_type = 'desktop',
    show_graph_range = false,
    show_graph_scale = false,
    stippled_borders = 0,
    update_interval = 1.0,
    uppercase = false,
    use_spacer = 'none',
    use_xft = true,
}

conky.text = [[
${color grey}Uptime:$color $uptime ${goto 500} CPU:   ${execi 5 sensors coretemp-isa-0000 | ag "Package id 0:" | cut -c 17-20}°C ${goto 800} ${time %r}
${color grey}Frequency (in GHz):$color $freq_g ${goto 500} GPU:   ${execi 5 sensors nvme-pci-7200 | ag "Composite:" | cut -c 16-19}°C
${color grey}RAM Usage:$color $mem/$memmax - $memperc% ${membar 4,100} ${goto 500} Water: ${execi 5 sensors highflownext-hid-3-7 | ag "Coolant temp:" | cut -c 25-28}°C
${color grey}Swap Usage:$color $swap/$swapmax - $swapperc% ${swapbar 4,100} ${goto 500} Flow:  ${execi 5 sensors highflownext-hid-3-7 | ag "Flow \[dL/h\]:" | cut -c 23-26 | awk '{$1=$1};1'} dL/h
${color grey}CPU Usage:$color $cpu% ${cpubar 4, 280} ${goto 500} Fans:  ${execi 5 sensors nct6798-isa-0290 | ag "fan3:" | cut -c 26-34 | awk '{$1=$1};1'}
${color grey}Processes:$color $processes  ${color grey}Running:$color $running_processes ${goto 500} Pump:  ${execi 5 sensors nct6798-isa-0290 | ag "fan6:" | cut -c 27-34 | awk '{$1=$1};1'}
${color grey}/ $color${fs_used /}/${fs_size /} ${fs_bar 4, 250 /}
${color grey}Up:$color ${upspeed} ${color grey} - Down:$color ${downspeed}
${color grey}Name              PID     CPU%   MEM%
${color lightgrey} ${top name 1} ${top pid 1} ${top cpu 1} ${top mem 1}
${color lightgrey} ${top name 2} ${top pid 2} ${top cpu 2} ${top mem 2}
${color lightgrey} ${top name 3} ${top pid 3} ${top cpu 3} ${top mem 3}
]]
